version: '2.1'

services:
  ramparitala-springboot:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: hmctspublic.azurecr.io/labs/ramparitala-springboot
    ports:
      - $SERVER_PORT:$SERVER_PORT
